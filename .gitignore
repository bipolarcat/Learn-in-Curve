# Reference codebase — has its own separate git repo, don't track it here
PMQ in 5 days/

# Discard pile — never part of the real project
JUNK/

# Node / build
node_modules/
.next/
out/
dist/
build/

# Env / secrets — never commit these
.env
.env.local
.env*.local

# OS/editor noise
.DS_Store
Thumbs.db

# Debug/build artifacts — not meaningful to keep in history
.cursor/debug-*.log
.cursor/checkpoint.log
tsconfig.tsbuildinfo
test-results/
playwright-report/

# Caption extraction/transcription scratch files (drafts and QA stay tracked)
scripts/captions/.tmp/

# Per-developer local overrides (per .cursor/skills/impeccable's own docs —
# this file is meant to be gitignored, .gitignore just never caught up)
.impeccable/config.local.json
*.mp4
!public/brand/hero/hero-animals-2.mp4
node_modules/
.claude/skills/

.vercel

# Wispr Flow dictation DB — temporary local copy for voice-guide analysis only.
# ~2GB, contains personal dictation history. Never commit. Delete after use.
_wispr/
*.sqlite
*.sqlite-wal
*.sqlite-shm
